bazel query 'kind("go_binary", //...)'

bazel query 'kind("go_binary", //backend/...)'

bazel query 'kind("go_binary", rdeps(//..., set(//common/common.go)))'

bazel query 'deps(//client:client)'

bazel query 'deps(//client:client)' --output build > deps.txt

bazel query //external:com_github_antchfx_xmlquery --output build 

endorctl scan --quick-scan

endorctl scan 

-> go run client/main.go (build is not working)

endorctl scan --use-bazel --bazel-targets-query="kind("go_binary", //...)"

endorctl scan --use-bazel --bazel-include-targets="//client:client" --bazel-show-internals
